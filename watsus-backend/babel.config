module.exports = {
  presets: [
    ['@babel/preset-env', { targets: { node: 'current' } }]  // Targeting the current version of Node
  ],
  plugins: ['@babel/plugin-transform-runtime']  // Helps to avoid duplication across your compiled output
};
